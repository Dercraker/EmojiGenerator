---
description: 
globs: 
alwaysApply: false
---
---
description: ENFORE rules
globs: *.store.ts, *.ts
alwaysApply: false
---
## Context

- The library used for handling authentification, organization and subscribtoin is `Better-Auth`.

## Config files

* @auth.ts is the main config file. We use `auth` only on the server
* @auth-client.ts is the client-side config file. We use `authClient` only on the client.
* @auth-permissions.ts is the file that define all the permissions for our application.
* @auth-plans.ts define the plans of the applications
* @auth-org.ts contain helper for the organization
